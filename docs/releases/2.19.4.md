# SD003 Framework 2.19.4

更新日: 2026-09-20。deployツールの版番号は3.5.0のまま。既存プロジェクトは`/sd-upgrade .`で取り込む。

## 変更内容

- `.claude/rules/skills/skill-trust-policy.md`: 「配置スコープ」節を追加。プロジェクト固有のスキル（とそのリンク）は `<プロジェクト>/.claude/skills/` に置き、`~/.claude/skills/` には全プロジェクトで使う汎用スキルだけを置く。リンク同期スクリプトの作り先と `.gitignore`、`disable-model-invocation: true` による自動発動の停止も明記した。

## 背景

at002 の会計スキル約200件へのリンクが `~/.claude/skills/`（全プロジェクト共通）に作られていた。原因は at002 の `sync-at002-skills.ps1`。そのため sd003 など無関係なプロジェクトでも会計スキルが候補に並び、誤って発動していた。at002 `0e4897ac` でリンクの作り先を at002 の `.claude/skills/` に変え、全体側の旧リンク201件は `~/.claude/.archive/skills-at002-links-20260920/` に退避した。

## 検証手順

```bash
# at002 では会計スキルが見え、sd003 では見えないこと
cd D:/claudecode/at002 && claude -p "スキル一覧に run-業務開始 はあるか。あり/なしで答えて"
cd D:/claudecode/sd003 && claude -p "スキル一覧に run-業務開始 はあるか。あり/なしで答えて"
python .codex/check-framework-version.py --source D:/claudecode/sd003
python scripts/sync-cli-commands.py --check
```
