# セッション記録

## セッション情報
- **日時**: 2026-06-07 17:00:10
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 952ef66 fix(deploy): deploy.sh overwrites settings.json unless .sd003-keep-protected (was skip-if-exists, left stale wiring un-upgraded)

## 作業サマリー

### 完了

（前半は session-20260607-164006.md に記録済み: Phase 6b検証ゲート実装・レビュー対応・at002修復・GEPA/SkillOpt批判レビュー。本記録は同日後半の続き。）

1. **nm002 を最新 sd003 が使える状態に更新（ユーザー依頼）**
   - verifier で導入状況確認 → 全体的に古い（新ガードレールhook7個欠落・rules4不足・skills大幅不足・廃止コマンド14残存・settings.json未配線）。
   - `/sd-upgrade --execute` で最新化（308新規追加、廃止物除去、全バックアップ退避）。
   - upgrade 後も settings.json が FAIL → 真因特定（後述 deploy.sh バグ）。hookファイルは全て揃ったので settings.json をテンプレで上書き → **verifier C1-C6 全PASS / exit 0**。
   - **CLAUDE.md は既存を尊重**（ユーザー指示）。再生成版とバイト一致で実害なしを確認の上、`nm002/.sd003-keep` に `CLAUDE.md` を登録し今後も保護。

2. **deploy.sh の settings.json 上書きバグを根本修正（nm002/at002 固着の真因）**（コミット `952ef66`）
   - 真因: `deploy.sh` は既存 settings.json を **SKIP**（`if [ -f ] then SKIP`）。deploy.ps1 は上書きするのに deploy.sh だけ SKIP → **upgradeしても古い/不完全な配線が直らない**。
   - 慎重に精読し、当初案「heredocをテンプレコピーに置換」は **誤り**と判明（heredocはOS対応の意図的生成器。Windows専用運用なら影響なしだが、最小変更を選択）。
   - 修正: SKIP を「`is_kept`（.sd003-keep）保護時のみSKIP、それ以外は再生成（上書き）」に変更。既存の KEPT_LOG/DIVERGED_LOG 機構に準拠。heredoc は維持。
   - 3シナリオ実機検証: 新規deploy=PASS / 既存の壊れたsettings.jsonに再deploy=UPDATE上書きでPASS（核心）/ .sd003-keep保護=KEEP維持。

### 進行中
（なし）

### 未解決

1. **他の現役配信先の棚卸し未実施**: nm002/at002 は完了。oc001/fw5yp/sb001/er001 等は未チェック。`/sd-upgrade <target> --dry-run` → `--execute` で最新化可能（settings.json も今後は自動上書きされる）。
2. **deploy.ps1/deploy.sh の二言語drift（settings.json）**: deploy.sh は OS対応heredoc、deploy.ps1 はテンプレ逐語コピー。Windows運用では両者ほぼ等価だが、settings.json の真実源が事実上3箇所（template/ps1コピー/sh heredoc）に分散している構造は残る。完全統一は別タスク（Windows専用なら優先度低）。

### 作成・変更ファイル

**変更（コミット済 952ef66）**
- `.claude/skills/sd-deploy/deploy.sh`（settings.json: SKIP→is_kept以外は上書き）

**nm002側（sd003リポジトリ外）**
- `D:\claudecode\nm002\` 全体: `/sd-upgrade` で最新化
- `D:\claudecode\nm002\.claude\settings.json`: 最新テンプレで全hook配線
- `D:\claudecode\nm002\.sd003-keep`: `CLAUDE.md` を追加（既存尊重・恒久保護）
- `D:\claudecode\nm002\.sd003-backup-20260607_164853\`: upgrade前の全上書き分バックアップ
- `D:\claudecode\nm002\.sd003-backup-settings-20260607_164440\`: 初回surgical試行時のバックアップ

### 使用した外部ファイル
- `D:\claudecode\nm002\`（最新化対象プロジェクト一式）
- `D:\claudecode\at002\.claude\settings.json`（前半セッションで修復済・本記録では参照のみ）

### 次回タスク

#### P0（緊急）
（なし）

#### P1（重要）
1. 残りの現役配信先（oc001/fw5yp/sb001/er001 等）を `/sd-upgrade` で最新化。各々 verifier で C1-6 PASS を確認。固有CLAUDE.mdがある場合は事前に `.sd003-keep` へ `CLAUDE.md` を登録。

#### P2（通常）
1. settings.json の真実源を1つに統一（template単一化＋deploy.sh/ps1から参照）。Windows専用なら優先度低。
2. nm002 等の `.sd003-backup-*` ディレクトリの整理（動作確認後）。

### 備考

- **deploy.sh修正の意義**: 「既存配信先を upgrade すれば settings.json も最新化される」状態を実現。これまでは hook ファイルは更新されても settings.json が SKIP され、ガードレールが不活性のまま固着していた（nm002/at002 がその実例）。
- **慎重対応の成果**: ユーザー「慎重に対応してくれ」を受け、当初の修正案（テンプレコピー化）を精読で否定。heredocのOS対応ロジックを壊さず最小変更（SKIP判定の1点）で解決した。思い込みで直さない原則の実践例。
- **CLAUDE.md尊重**: deploy/upgrade はデフォルトで CLAUDE.md をテンプレ再生成する。固有版を持つ配信先は `.sd003-keep` への `CLAUDE.md` 登録が必須。nm002 は対応済み。

### 学習ナッジ（修正が2回以上）
- 修正3回検出
- 修正内容:
  1. **CLAUDE.md再生成の是非**: upgradeでCLAUDE.mdをテンプレ再生成したが、ユーザー指示「CLAUDE.mdは既存のものを尊重しろ」→ `.sd003-keep`で保護。**配信先の固有CLAUDE.mdは原則保護すべき**という知見。
  2. **settings.json単独修復の不十分さ**: nm002はhookファイル自体が欠落しており settings.jsonコピーだけでは不足→full upgradeが必要だった。「修復スコープは実測してから決める」。
  3. **修正案の事前否定（テンプレコピー化）**: ユーザー「Linux/Macデプロイが壊れます」（後にWindows専用と判明）の前に、自分の精読でheredocのOS対応意図に気づき当初案を撤回。**コード変更前の精読で誤った修正を未然に防いだ**。
- 永続化提案: auto-memory feedback に「deploy/upgradeは配信先の固有CLAUDE.mdをテンプレ再生成で上書きする→`.sd003-keep`に`CLAUDE.md`登録で保護」を記録検討。
