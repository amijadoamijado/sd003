# セッション記録

## セッション情報
- **日時**: 2026-09-20 01:41:11
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 249dc01 release: SD003 2.19.4 (skill placement scope: project skills stay in project .claude/skills)

## 作業サマリー

### 完了
1. ユーザーの質問「スキルをプロジェクト内だけで使えるか」に回答した。置き場所で効く範囲が決まる（`~/.claude/skills/` は全プロジェクト、`<PJ>/.claude/skills/` はそのPJだけ）。ほかに `disable-model-invocation: true` と `permissions.deny` の `Skill(名前)` がある
2. 原因を実測した。`~/.claude/skills/` の210件のうち201件が at002 会計スキルへのリンク（Junction）だった。作っていたのは at002 の `skills/scripts/sync-at002-skills.ps1`（Claude の作り先が `$HOME\.claude\skills`）
3. at002 を修正した（at002 `0e4897ac`、push済み）
   - 同期スクリプトの作り先を `D:\claudecode\at002\.claude\skills\` に変え、「全体側へ戻さない」旨をコメントに書いた
   - `.gitignore` に `/.claude/skills/{run,wrap,assign,ref,check}-*` を追加した
   - `CLAUDE.md`・`AGENTS.md` の `~/.claude/skills/...` 表記を `.claude/skills/...` に合わせた
   - 新しい場所にリンク200件を作成した
   - 全体側の旧リンク201件は削除せず `C:\Users\a-odajima\.claude\.archive\skills-at002-links-20260920\` へ移した（Move-Item でリンクのまま移動）。うち2件（`run-給与仕訳作成-your`、`run-総武三菱仕訳`）は元から本体が無い壊れたリンクだった
4. 実際に起動して確認した（`claude -p`）。at002 では `run-業務開始` と `check-AMEX明細抽出品質` が「あり」、sd003 では「なし」
5. SD003 2.19.4 をリリースした（`249dc01`、push済み）
   - 配布ルール `.claude/rules/skills/skill-trust-policy.md` に「配置スコープ」節を追加した
   - 版番号を3系統（`.claude`/`.agents`/`.grok`）の deploy.ps1・deploy.sh・SKILL.md・テンプレートと CLAUDE.md の末尾行で揃えた
   - 変更記録 `docs/releases/2.19.4.md` を新設した
   - 検証: `check-framework-version.py` は current 2.19.4、`sync-cli-commands.py --check` は OK（21件）、スクラッチパッドへの試験 deploy で新しい節と v2.19.4 が入ったことを確認

### 進行中
- なし

### 未解決
- 既存プロジェクトへの 2.19.4 反映（`/sd-upgrade .`）は未実施。ユーザーに実施するか尋ねたが、回答はまだ無い

### 作成・変更ファイル
- sd003: `.claude/rules/skills/skill-trust-policy.md`、`.claude|.agents|.grok/skills/sd-deploy/{deploy.ps1,deploy.sh,SKILL.md,templates/CLAUDE.md.template}`、`CLAUDE.md`、`docs/releases/2.19.4.md`
- at002: `skills/scripts/sync-at002-skills.ps1`、`.gitignore`、`CLAUDE.md`、`AGENTS.md`
- 全体: `C:\Users\a-odajima\.claude\skills\` から at002 向けリンク201件を退避

### 使用した外部ファイル
- `D:\claudecode\at002\skills\scripts\sync-at002-skills.ps1`（リンク同期スクリプト。修正済み）
- `D:\claudecode\at002\.claude\skills\`（新しいリンクの置き場所）
- `D:\claudecode\at002\CLAUDE.md`、`D:\claudecode\at002\AGENTS.md`、`D:\claudecode\at002\.gitignore`、`D:\claudecode\at002\.sd003-keep`（参照のみ）
- `C:\Users\a-odajima\.claude\skills\`（全体共通スキル。残りは9件）
- `C:\Users\a-odajima\.claude\.archive\skills-at002-links-20260920\`（旧リンクの退避先）

### 次回タスク

#### P0（緊急）
- なし

#### P1（重要）
- 各プロジェクトへ 2.19.4 を反映するか、ユーザーの判断を確認する。反映するなら各PJで `/sd-upgrade .`。at002 は `.sd003-keep` で `.claude/rules` を保護しているのでルールは入らない（リンクの修正は済んでいるので問題ない）

#### P2（通常）
- 全体側に残った9件（agent-browser / akapen / archive-sessions / cleanup-disk / codex-dispatch / gemini-dispatch / notebooklm-slide-pipeline / organize-desktop / synced）も、特定PJ専用にするか検討する
- at002 のスキル本文（`skills/**/*.md` 約40ファイル）やスクリプトのコメントに `~/.claude/skills/ref-OCRエンジン選択` などの旧パス表記が残っている。動作には影響しない

### 備考
- at002 以外のフォルダ（顧客フォルダ等）で起動すると会計スキルは出ない。at002 の中で起動して使う
- sd003 の hook は `git add -A <paths>` を「リポジトリ全体のステージ」としてブロックする。パス指定の場合も `-A` を付けないこと
- 修正はユーザーから受けていないので、学習ナッジは無し
