# セッション記録

## セッション情報
- **日時**: 2026-04-20 16:56:16
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 2ce102b session: 全33PJ .kiro退避 + 9PJ /sd-deploy + 71件レガシー駆除 + 119ファイル sed 置換 + ta001整合性復旧

## 作業サマリー

### 完了

1. **全33プロジェクトのGemini CLI TOMLパースエラー一括解消**
   - 対象: `D:\claudecode\*\.gemini\commands\*.toml`（全1236ファイル）
   - 修正前: 22プロジェクトで253件のパースエラー、16プロジェクトで225件のBOM残存
   - 修正後: エラー0件・BOM残存0件
2. **3種類の修正を自動化**
   - BOM除去のみ: 196ファイル
   - `prompt = """` → `prompt = '''`（リテラル文字列化でバックスラッシュエスケープ不要化）: 28ファイル
   - 文字化けファイルを他プロジェクト健全版で置換（ad001等をソース）: 29ファイル
3. **事前安全チェック実施**
   - `'''` コリジョンチェック（prompt内に`'''`が含まれるファイルなし、swap安全）
   - ソース側（ad001）のプロジェクト固有パス混入なしを確認
4. **修正スクリプト `/tmp/fix_toml_apply.py` で一気通貫実行**
   - 第1パス: ファイル名別に「健全な兄弟」を収集
   - 第2パス: BOM除去 → parse → swap → parse → sibling置換 の順で段階的修復

### 進行中
なし

### 未解決
なし

### 作成・変更ファイル

**TOML修正（253ファイル）**
- 22プロジェクト横断: PC001, SB001, ad001, as001, at001, cf001, ck001, cr001, er001, fw000, fw001, fw002, fw003, fw5yp, nm001, nm002, ob001, pm002, sd002, sd5yp, sp001, ta001-standalone
- sd003は前回セッションで修正済み（コミット 1b1d288）

**本セッションで生成（一時）**
- `/tmp/fix_toml.py`（検証用・コミットなし）
- `/tmp/fix_toml_apply.py`（実行用・コミットなし）

### 次回タスク

#### P0（緊急）
なし

#### P1（重要）
1. 各プロジェクトでgit add + commit実行（今回は検出・修復のみ、22プロジェクトの状態は未コミット）
2. `/sd-deploy` 時のTOMLテンプレート生成時に`"""`ではなく`'''`を使用する規約追加の検討（再発防止）

#### P2（通常）
1. 文字化け発生源の特定（どの時点でエンコード破壊が起きたか）
2. deploy.ps1の文字化け予防策（Out-File -Encoding utf8NoBOM 等の徹底）

### 備考

**再発防止の観点**
- BOM混入はPowerShellの`Out-File`デフォルト挙動が原因の可能性が高い。deploy時は`-Encoding utf8NoBOM`明示必須
- `"""` (basic multiline) はWindowsパス `D:\...` をエスケープ必須にする罠。`'''` (literal multiline) が正解

**スコープ判断**
- 22プロジェクトのgit commitは各プロジェクト個別の判断が必要なため、自動コミットしない方針で完了報告
- 修正自体はローカルに適用済み、次回gemini起動時から効果発生

### 学習ナッジ（修正が2回以上ある場合のみ記載）
- 修正1回検出（初回応答で「sd003のみ確認済み」と誤回答→ユーザー指摘「すべてのプロジェクトで対応しているのか」で横展開実施）
- 修正内容:
  1. 修正対象の認識不足: ユーザーが示した「このようなエラー」は特定プロジェクトの症状ではなく、全プロジェクトに波及している可能性を初動で確認すべきだった
- 永続化提案: 「前セッションの他AI修正を検証する場合、同じ修正パターンが他プロジェクトにも適用されているか横断確認を初動で実施」をfeedbackとしてauto-memory化を検討
