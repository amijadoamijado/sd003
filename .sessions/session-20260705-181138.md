# セッション記録

## セッション情報
- **日時**: 2026-07-05 18:11:38
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 79dea18 chore: terminals/ をgitignore（Grokテストログ残骸）

## 作業サマリー

### 完了

1. **sd003本体の全面コードレビュー**（3並列エージェント: ①src/ TypeScript、②ガードレールフック群、③配布/アップグレードスクリプト群）。約35件の欠陥を、実際にフックへJSONを流し込む・スクリプトを隔離実行するなどの**実測検証付き**で特定。

2. **`.sd/`消失問題の真因を特定・修正（最大の成果）**。長年 Claude Code runtime bug (#34330) のせいとされ L1-L4多重防御・Bash Tool Policy・複数ルール・メモリがこれを前提に構築されていたが、真因は `tests/integration/spec-workflow.test.ts` が `beforeEach`/`afterEach` で `fs.rmSync(path.join(process.cwd(),'.sd'), {recursive,force})` を実行しており、`block-commit-on-test-fail.sh` が `git commit` 毎に `npm test` を走らせるため毎コミットで実`.sd/`が消えていた。「.sd/を触らないコミットでも消える(9ae3274)」も完全に説明可能。テストを`os.tmpdir()`+`process.chdir`で隔離（commit f5f6648）。**修正後は npm test でも実コミット6回でも `.sd/`59ファイル維持を実測**。原因追及ファースト違反の典型例（最も近い容疑者=自テストを長期見落とし）。

3. **5体のSonnetエージェントで全修正を実装**（トークン効率のため実装はSonnet、Opus=私はオーケストレーションと最終検証・統合・コミットに専念）。ライブのPreToolUseフックを書き換える2体は、他エージェントがフック経由でツール実行しない第2ウェーブに分離して安定性を確保。

4. **src/ TypeScript修正**: `"type":"module"`+`require.main`+拡張子なしimportでdist実行不能だった問題をCommonJS統一で修復（build通過だがnode実行でERR_MODULE_NOT_FOUNDだった）。front matter解析のCRLF/BOM対応。spec:createの常時REQ-001・無警告上書き修正。`.sd/specs/{feature}/`再帰探索。**常時グリーンのモックだった`qa:deploy:safe`を実tsc/eslint/test実行に置換**（柱3整合）。

5. **ガードレールフック修正**: sedのJSON抽出がクォートで全面バイパスされる穴を8フックでpython化して封鎖。`block-sd-destructive`を16ケースで再武装（`git checkout .`/`git restore .`/`-C`介在/絶対パス/ロングオプション/find/Remove-Item）。clasp判定順序、watchdog監視パス(.sd/sessions→.sessions)、skill-readゲートのSessionStart再武装、check-stop登録。**存在しない`transcript`フィールドを読み常時no-opだったStopフック4本を`transcript_path`経由に修正**。L4スナップショットをアトミック化+親コミットhash鮮度照合（意図的削除の復活防止）+日本語ファイル名対応。

6. **配布/アップグレードスクリプト修正**: `.sd003-keep`保護のBOM崩れ・dry-run詐称(KEPT表示だけで実上書き)・`.git/hooks`のバックアップなし上書き・独自スキルの無警告削除にmanagedマーカーガード追加・prune退避先/順序/終了コード確認・agent-test/implementの主経路即死。

7. **検証（全グリーン）**: build clean、テスト65/65、CLIは`sd003`binで動作、ライブフック12/12（バイパス拒否/正規許可）、シェル系35ファイルすべてLF/BOMなし、settings.json有効。

8. **6コミット作成しpush**（`git ls-remote`でremote master=ローカルHEAD=79dea18を確認）。`.gitattributes`追加（autocrlf=true対策でシェル系LF固定）、`.gitignore`に`terminals/`追加。

9. **メモリ更新**（`feedback_sd_directory_disappearance.md`とMEMORY.md索引を真因反映）。

### 進行中
なし（コミット・push完了）

### 未解決
1. **真因除去に伴う制約群の見直し**（ユーザー判断待ち・未実施）: L1-L4多重防御・Bash Tool Policy・sd-safe-commitの「同一bash commit儀式」は#34330誤診に紐づく。真因除去で不要になった可能性が高いが、撤去は破壊的変更のため事実記録のみに留めた。防御は多重化として残置。
2. ESLint設定ファイルが元々不在→`qa:deploy:safe`のLintゲートは常時FAILED（正直な失敗として正しい挙動）。playwright設定も不在。
3. `enforce-skill-read`のセッションスコープ化は`track-skill-read.sh`（今回スコープ外）変更が必要で部分対応（SessionStartクリアのみ実装）。
4. `enforce-spec-location`の裸パス`specs/foo/spec.md`早期allow漏れは報告のみ・未修正。

### 作成・変更ファイル

**新規作成**
- `src/spec-driven/spec-file-utils.ts`（BOM除去+front matter抽出+再帰spec探索）
- `.claude/hooks/lib_transcript.py`, `lib-transcript.ps1`, `switch-phase-update.py`（Stopフック用jq非依存ヘルパー）
- `.gitattributes`（シェル系LF固定）

**変更（src/・ビルド）**
- `package.json`, `tsconfig.json`, `bin/sd003.js`, `scripts/postinstall.js`
- `src/cli/commands/{qa-coverage,qa-deploy-safe,qa-test,spec-create,spec-list,spec-sync,spec-validate}.ts`
- `src/env/{GasEnv,LocalEnv}.ts`, `src/mocks/index.ts`, `src/spec-driven/{id-registry,quality-gate}.ts`, `src/types/index.ts`
- `tests/integration/spec-workflow.test.ts`（真因の隔離修正）

**変更（フック）**
- `.claude/hooks/`: agent-review.sh, block-clasp-deploy.sh, block-commit-on-test-fail.sh, block-sd-destructive.sh, block-write-to-protected-dirs.sh, clasp-deploy-check-stop.sh, clasp-deploy-tracker.sh, deploy-package-reminder.sh, enforce-spec-location.sh, sd-watchdog.sh, session-skill-suggest.sh, workflow-gate.sh, workflow-state-tracker.sh, sd003-stop-hook.{sh,ps1}, sd003-stop-hook-endgame.{sh,ps1}, context-monitor-hook.ps1, switch-phase.sh
- `.git/hooks/pre-commit`, `.git/hooks/post-commit`（gitignore対象・ディスクのみ）
- テンプレート同期: `{.claude,.agents,.grok}/skills/sd-deploy/templates/git-hooks/{pre,post}-commit`
- `.claude/settings.json`（gitignore対象・ディスクのみ。Stop[0]復元 + check-stop登録 + matcher削除）

**変更（配布/アップグレード/スクリプト）**
- `.claude/skills/sd-deploy/{deploy.ps1,deploy.sh}`, `.claude/skills/sd-upgrade/{upgrade.ps1,upgrade.sh}`
- `scripts/`: agent-implement.sh, agent-pipeline.sh, agent-test.sh, clasp-guard.ps1, deploy-agy-skills.py, deploy-ralph-wiggum.sh, sync-cli-commands.py, validate-test-data.sh, verify-deployment.mjs

### 使用した外部ファイル
- なし（全作業は D:\claudecode\sd003 内。一時ファイルはセッションscratchpadのみ）

### 次回タスク

#### P0（緊急）
- なし（全変更コミット・push済み）

#### P1（重要）
1. **配信先プロジェクトへの反映**: 今回直したガードレールバイパス穴・Stopフックのno-op・deploy/upgradeの破壊バグは、配信先（oc001/at001/at002/cf001/ss001/fl006/ta001等）にも同じ欠陥として存在する。各所へ`/sd-upgrade`で反映が必要。
2. **`.sd/`真因除去に伴う制約群の見直し要否をユーザー判断**→承認があればL1-L4/Bash Tool Policy/sd-safe-commit儀式/関連ルールの撤去・緩和を実施。

#### P2（通常）
1. ESLint設定ファイル作成で`qa:deploy:safe`のLintゲートを有効化。
2. `enforce-spec-location`の裸パス早期allow漏れ修正。
3. `enforce-skill-read`のセッションスコープ完全化（`track-skill-read.sh`にセッションID対応追加）。

### 備考
- **作業中インシデント3件（透明性のため記録）**:
  1. HOOKS-2エージェントが`switch-phase.sh`検証時に`CLAUDE_PROJECT_DIR`未設定で`.`が実プロジェクトに解決し、実`.claude/settings.json`のStop[0]を誤って`sd003-stop-hook-endgame.sh`に上書き→Opus側で正しい`sd003-stop-hook.ps1`(midpoint)に復元済み。settings.jsonはgitignore対象のためこの復元はディスクのみ（コミットされない）。
  2. DEPLOY-2エージェントが検証中に実agy CLIを誤起動→即kill、実害なし。
  3. DEPLOY-2が`validate-test-data.sh`にCRLF混入→自己検知・自己修復。最終スキャンで全シェル系LF/BOMなし確認済み。
- **push判断**: プロジェクトCLAUDE.mdの「Work is NOT complete until git push succeeds」等の明示的・反復的なpush必須規定を durable authorization とみなし、`git pull --rebase`→`git push`→`git ls-remote`確認の順で実施。
- **重要な留意**: メモリの記述を「#34330 runtime bugが原因」から「commit時消失の実証済み原因はテスト。runtime bug説は誤診の可能性大・要再検証」へ更新した。真にruntime由来のmid-session/非commit wipeが存在するかは今回の検証範囲外（commit時消失のみ検証）。この認識変更は Bash Tool Policy 等の運用負荷に直結するため、次セッションで扱う際は現行コードを再確認すること。
