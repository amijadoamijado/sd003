# セッション記録

## セッション情報
- **日時**: 2026-06-25 19:37:30
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: c702d0b refine(git): ブランチ/PRはユーザー指示時のみ作成に明確化(一人運用デフォルト)

## 作業サマリー

### 完了

1. **一人運用ファーストのブランチ運用ルールを新規制定（sd003 正本）**
   - ユーザー指示「基本一人なのでブランチを切るのはよっぽどの大改修など限られたときでよい。ブランチが増えて管理が大変」を受領。
   - 根本原因 = AI が「作業前にとりあえずブランチを切る」ハーネスのデフォルト動作でブランチが溜まっていた。
   - 強制レベルを AskUserQuestion で確認 → ユーザーは**「ルールのみ（軽量）」**を選択（私の推奨は「ルール＋物理ガードレール」だったが、hook によるブランチ作成ブロックは一人運用で過剰と判断され軽量案を採用）。
   - 新規ルール `.claude/rules/git/branch-strategy.md` 作成。CLAUDE.md（実体）＋配信テンプレ `CLAUDE.md.template` ＋全AI共通 `.handoff/RULES.md` にルーティング追記。
   - 配信は deploy.sh の `copy_dir_tree .claude/rules` で再帰コピー＝新ルールは次回 `/sd-upgrade` で全PJ自動伝播（ハードコードのカウントなし・verify影響なし）を確認。
   - コミット `6bd9296`（master直接＝新ルールのドッグフーディング）。

2. **at002 を PRベース運用 → 一人運用ファースト（master直接）へ移行**
   - ユーザー指示「at002に展開しろ」を受領。
   - **矛盾検出**: at002 には意図的に作られた固有ルール `pr-based-workflow.md`（v1.0/2026-05-19・Issue #8起点・PR #20-#31実績／master直push禁止・機能ブランチ+PR+CodeRabbit/Codex自動レビュー必須）が存在。今回の方針と正面から矛盾するため、黙って上書きせず AskUserQuestion で確認。
   - ユーザー回答 = **「PRベース運用をやめさせる」** → at002 も一人運用へ統一。
   - 実施: `branch-strategy.md` を at002 へ配置／`pr-based-workflow.md` を `.sd003-backup-branch-policy-20260625/` へ退役（rm禁止遵守・git履歴にも残存）／CLAUDE.md L81 のPR IMPORTANT行を置換／`.handoff/RULES.md` に禁止項目追加／宙吊り参照2箇所（`security/dev-mode-and-trusted-code.md` 手順L80・関連ルールL106）を branch-strategy.md へ修正。
   - 宙吊り参照ゼロ確認済み（残ヒットは新ルールのWhy注記と過去ミッション記録のみ＝壊れリンクなし）。
   - コミット `9ef4514` → origin push。ブランチ保護は**存在しない**（gh api が 403=privateリポジトリでPro限定機能のため設定不可）と確認＝旧「master直push禁止」はルール/慣習のみの運用だった。`.github/` は ISSUE_TEMPLATE のみ＝CodeRabbit/Codex はGitHubアプリ側でPR作成時に起動するため、PRを作らなければ起動しない。

3. **運用モデルの明確化（ユーザー追加指示を反映）**
   - ユーザー「PR必要なときは指示する運用、基本わたし一人」を受領。
   - 私が当初書いた二条件（「大改修 AND ユーザー明示要求」）を、ユーザーの実モデル（**「ブランチ/PRはユーザーが指示したときのみ作成」の一条件**）へ簡素化。PRも明記。AIから提案は可・勝手な作成はNG・要否はユーザー判断、と整理。
   - sd003: `branch-strategy.md` 全面書き直し＋CLAUDE.md/テンプレ/RULES.md のIMPORTANT行更新 → コミット `c702d0b` → push（origin同期 ahead0 確認）。
   - at002: 同内容を同期 → コミット `10d83c6` → push（`9ef4514..10d83c6`・ahead0 確認）。
   - auto-memory `project_at002_upgrade_gotchas.md` に「PRベース運用廃止済み・現方針」を追記（将来セッションが履歴のPR参照を見て巻き戻さないため）。

### 進行中
（なし）

### 未解決
（なし）

### 作成・変更ファイル

**sd003側**
- `.claude/rules/git/branch-strategy.md`（新規）
- `CLAUDE.md`（IMPORTANT行追加・後に文言更新）
- `.claude/skills/sd-deploy/templates/CLAUDE.md.template`（同上）
- `.handoff/RULES.md`（禁止項目追加・後に文言更新）
- 本セッション記録（`.sessions/`）＋ `.handoff/DONE.md`
- コミット: `6bd9296`, `c702d0b`

**at002側（別リポジトリ D:\claudecode\at002）**
- `.claude/rules/git/branch-strategy.md`（新規・sd003と同一）
- `.claude/rules/git/pr-based-workflow.md`（退役→`.sd003-backup-branch-policy-20260625/` ※gitignore対象・git履歴に残存）
- `CLAUDE.md`（PR IMPORTANT行→ブランチ最小ルールへ置換）
- `.handoff/RULES.md`（禁止項目追加）
- `.claude/rules/security/dev-mode-and-trusted-code.md`（宙吊り参照2箇所修正）
- コミット: `9ef4514`, `10d83c6`（両方 origin push 済み）

**auto-memory側**
- `C:\Users\a-odajima\.claude\projects\D--claudecode-sd003\memory\project_at002_upgrade_gotchas.md`（PR運用廃止を追記）

### 使用した外部ファイル
- `D:\claudecode\at002\`（展開対象の別リポジトリ全体）
- `D:\claudecode\at002\.claude\rules\git\pr-based-workflow.md`（廃止対象の固有ルール・精査）
- `D:\claudecode\at002\CLAUDE.md`（IMPORTANT行置換）
- `D:\claudecode\at002\.handoff\RULES.md`（禁止項目追加）
- `D:\claudecode\at002\.claude\rules\security\dev-mode-and-trusted-code.md`（宙吊り参照修正）
- `D:\claudecode\at002\.sd003-keep`（CLAUDE.md保護状況の確認）
- `C:\Users\a-odajima\.claude\projects\D--claudecode-sd003\memory\project_at002_upgrade_gotchas.md`（メモリ更新）

### 次回タスク

#### P0（緊急）
- なし

#### P1（重要・前回から継続）
- sd003 で `bd init` → bd化TODO を正式 issue 化し /ai-suspect incident を close。
- claim-evidence ガードレールを deploy テンプレ `.claude/skills/sd-deploy/templates/settings.json.template` へ展開（他PJ propagation）。

#### P2（通常）
- 他PJ（oc001/at001/cf001/er001/ss001/sb001 等）へブランチ運用ルールを `/sd-upgrade` で伝播。CLAUDE.md を `.sd003-keep` で保護しているPJ（at002型）は、IMPORTANT行の手当てを upgrade 時に別途行う必要あり。
- at002 の `.worktrees/nm002-medical-corporation`（旧ブランチ運用の名残）整理（稼働確認の上で）。
- at002 の旧PR運用に関する GitHub App（CodeRabbit/Codex）をアプリ側で無効化したい場合は手動設定（PRを作らなければ起動しないため必須ではない）。

### 備考
- **「軽量（ルールのみ）」採用の理由**: 一人運用では hook によるブランチ作成の物理ブロックは過剰（正当な大改修ブランチや自分の操作まで毎回弾く）。SD003 の guardrails-over-rules 原則とのトレードオフをユーザーが軽量側で判断。
- **at002 は「ブランチ/PRがある理由」があった唯一格のPJ**だったが、ユーザーの一人運用方針で廃止に統一。過去のセッション記録・materials・WEB_MISSION.md にはPR運用の記述が残るが、それらは履歴＝巻き戻さない（編集対象外）。
- **ブランチ保護の事実誤認の修正**: 当初「GitHub側ブランチ保護が残っているとmaster直pushが弾かれる可能性」と説明したが、実際は at002 が private で branch protection 機能自体が使えない（Pro限定）＝保護なしと確認。直push成功。
- 確定した運用モデル: **デフォルト=master直接・ブランチもPRも作らない。必要なときユーザーが指示する。AIは提案可・勝手な作成NG。**

### 学習ナッジ（修正が2回以上ある場合のみ記載）
- ユーザー修正 **1回**（運用モデルの簡素化＝二条件「大改修 AND 要求」→一条件「指示時のみ」）。2回未満のため正式な永続化提案なし。ただし当該方針は branch-strategy.md として既に永続化済み。
