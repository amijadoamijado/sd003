---
name: session-write
description: "Legacy alias for sessionwrite (SD003 command /sessionwrite). Use when the user runs /session-write."
---

# session-write (alias)

This skill is an alias of `sessionwrite`. Follow the same steps as `sessionwrite` to reproduce `/sessionwrite`.

User-provided arguments (if any): $ARGUMENTS
