# セッション記録

## セッション情報
- **日時**: 2026-07-25 17:00:18
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: b19cca7 chore(sd003): resync agy/Grok deploy.sh mirrors (FRAMEWORK_VERSION 2.17.0 + gitignore phase)

## 作業サマリー

### 完了

1. **Codex自動レビューhookの4か月無音故障を修正（B16）**
   - 症状: 全コミットで `.codex-review-result.md` に「Codex CLI exited with code 2」のエラースタブだけが生成される（16PJで確認）
   - 真因: commit `231aca3`(2026-03-31)で `codex exec --full-auto` → `codex review --commit <SHA> "<PROMPT>"` に移行したが、
     codex CLI は `--commit` と `[PROMPT]` が**排他**（`--uncommitted` も同様）。exit 2 で即死していた。
     さらに `2>/dev/null` が stderr を捨てていたため、1秒で判明するはずのエラーが4か月生き延びた
   - 修正: 正準 `codex exec`（`RUST_LOG=error` / `-c model_reasoning_effort=medium` / `--sandbox read-only` / `-o` / `2> progress.log`）へ復帰。
     失敗時は stderr の tail を成果物に必ず添付。同じ壊れ方の `scripts/agent-review.sh` / `scripts/agent-pipeline.sh` も修正
   - 設計判断: 実レビューは2行diffでも3〜4分かかり PostToolUse hook は同期実行（timeout=600）のため、
     **既定OFF・opt-in（`SD_AUTO_REVIEW=1`）** ＋上限300秒に変更（ユーザー選択）
   - 実機検証: `SD_AUTO_REVIEW=1` で実行 → 2026-03-08以来はじめて実レビュー生成（Warning 2/Info 1）

2. **自動pushの無音失敗を可視化（B17）**
   - `nohup git push ... >/dev/null 2>&1 &` が終了コードも出力も破棄していた
   - `.git/auto-push.log` へ必ず記録、失敗時は `.git/auto-push-failed` マーカーを残し**次回コミット冒頭で警告**
   - 実機検証: `[2026-07-25 13:46:02] branch=master commit=5e4f5e0 exit=0` / `4db367a..5e4f5e0` を記録

3. **deploy の .gitignore 処理を整備**
   - deploy.ps1 Phase 5-5b に `.codex-review-result.md` を追加
   - **deploy.sh には .gitignore 処理が丸ごと欠けていた**（parity gap）ため新規実装。
     sh経由の配信先は `.claude/settings.json` すら未登録＝`.sd/`消失バグの予防線が無かった
   - FRAMEWORK_VERSION 2.16.0 → 2.17.0（ps1/sh/AIミラー同期）

4. **版数刻印の穴4件を塞いだ（ユーザー選択: 無音4件だけ手当て）**
   - ta001 `v3.2.0`（テンプレのリテラル残骸で「現行より新しい」と誤判定）→ `v2.16.0` に修正
   - at002 / fl006 / ac001 は刻印なし（`.sd003-keep` でCLAUDE.md保護のため deploy が書けなかった）→ 刻印追加
   - ac001 には Step 6 入り `sessionread.md` も配備
   - 版数は推測せず、各PJ配信済み `deploy.ps1` の `$FRAMEWORK_VERSION` を実測して刻印
   - 検証: 4件とも `/sessionread` で「v2.14〜2.16 → v2.17.0」の通知が出る状態を Step 6 ロジック再現で確認

5. **2026-03-28 一括移行による18PJ・229ファイルの文字化けを発見し全件復元**
   - 発見経緯: ac001 の CLAUDE.md 文字化け調査 → 同一メッセージのコミットが18リポジトリに存在（3,155ファイル書き換え）
   - 壊れ方: UTF-8をCP932として読み書き。**日本語だけでなくASCII文字と改行まで欠落**
     （`api_quota: "APIクォータ",` → `api_quota: "API,` / 826行→728行）。文字コード変換では復元不能
   - 復元: Tier A（移行直前リビジョン + `.kiro→.sd` 再適用）226件 / Tier B（SD003の2026-03-28時点）3件
   - 安全設計: 「移行以降そのファイルが未変更」を主柱ゲート（＝現在の中身は親から機械的に生成されたものしか含まない）。
     移行後コミットがある場合も差分がリネームのみなら機械判定して適用。
     リネーム再適用の要否は「原文のまま」vs「置換後」で**破損版に残ったASCIIパストークン数に近い方**を機械選択
   - 検証: 8,872ファイル再走査で**文字化け残存0件**

6. **復元結果を16PJにコミット（ユーザー選択: クリーンな16PJのみ）**
   - 無関係な未コミット変更は一切巻き込まず選択stage（er001/fl006/sb001 で計15件を除外）
   - 14PJが push 完了。pc001 は remote に `cc66b47 test commit` があり divergence → `git pull --rebase` → push

7. **Playwrightバイナリ混入によるpush恒久不能を3PJで解消（履歴書き換え）**
   - 真因: HTTP 408 の正体は Chromium バイナリ（chrome.dll 246〜254MB）のgit混入。GitHubの1ファイル100MB上限で**構造的にpush不可能**
   - fw003 `pw-browsers/` 529MB→1.4MB / oc001 `.playwright-browsers/` 700MB→134MB / ac001 `node_modules/`（15,511ファイル丸ごとtracked）→3.6MB
   - 手順: bundle バックアップ → `git filter-repo --invert-paths` → `.gitignore` 追記 → origin再設定 → force push → upstream再設定
   - oc001 は取り残されていた19コミットが全てリモートに乗った

8. **ac001 のクリーン化と復元再適用（ユーザー指示「消えてもいい」）**
   - 未コミット99件を scratchpad へバックアップ後にクリーン化
   - `git reset --hard`/`git clean`/`git restore` は sd003 ガードレールが物理ブロック
     → HEAD blob のファイル単位書き戻し + untracked 退避移動 + `git add --renormalize` で等価クリーン化
     （「消えないM」の正体は autocrlf のEOLフラグ＝内容差ゼロ）
   - 文字化け復元39件を選別再適用して commit + push
   - 追加発見: `kiro-deploy.md` / `kiro-init-project.md` は**移行前から化けていた**（別の過去事故）→ 2026-02-08 / 2025-10-19 のクリーン版から復元
   - 最終検証: 275ファイル走査・文字化け残存0件・リモート一致

9. **CLAUDE.md が死んだGrokモデルを既定案内していた問題を修正**
   - 「default `grok-build`」→ `grok-build` は 2026-07-12 に unknown model id 化。`grok models` 実測で現存は `grok-4.5` のみ
   - ラッパー `grok-run.ps1` は修正済みだったが CLAUDE.md だけ古い記述が残っていた

10. **deploy.ps1 の optional skills 除外不一致を修正**
    - `4db367a` が Phase 6 検証側だけに `-Exclude $optionalSkills` を足し、コピー側は全件配布のままだった
      → 必須ファイルが欠落しても件数比較でPASSしうる状態
    - コピー側（`.agents\skills` / `.grok`）にも除外を適用。テスト88件全通過
    - **復活させた自動レビューが初日に検出した実バグ**

11. **監査で異常なしを確認した項目**
    - sd003 の hook 配線: settings.json が参照する21本すべて実在
    - 外部CLI呼び出しで stderr を捨てている箇所: 0件
    - `codex-run.sh` / `agy-run.ps1`: 禁止フラグ混入なし
    - 版数刻印と実体の不一致: nm002 の1件のみ（軽微・通知は出る）

### 進行中
なし（着手した項目はすべて完了）

### 未解決

1. **44PJが壊れた自動レビューを回し続けている**
   - 配信先は旧 `agent-review.sh`（常時ON・`codex review --commit`）のまま。コミットのたびにエラースタブを生成
   - うち38PJは `.gitignore` 未登録のため未追跡ファイルとして出続ける
   - 止めるには当該PJで `/sd-upgrade`。ユーザー選択で「今はsd003のみ」としたため未実施

2. **50MB超ファイルをブロックする物理ガードレールが未整備**
   - 100MB超を一度でもcommitすると履歴書き換えまでpush恒久不能になるが、入口で止める仕組みが無い

3. **oc001 に切れたシンボリックリンク**
   - `.claude/skills/webapp-testing` → `.agents/skills/webapp-testing`（存在しない）。git status が warning を出す

4. **nm002 の版数刻印が実体より1つ古い**（2.14.0 vs 2.15.0）。CLAUDE.md が `.sd003-keep` 保護のため。実害小

5. **deploy.sh に optional skills 除外機能が無い**（ps1との機能差）。sh単体では整合しているため実害は無いが、
   ps1経由とsh経由で配信内容が変わる

### 作成・変更ファイル

**sd003（コミット済み・push済み）**
- `.claude/hooks/agent-review.sh`（invocation修正・opt-in化・stderr可視化）
- `scripts/agent-review.sh` / `scripts/agent-pipeline.sh`（同型の壊れ方を修正）
- `.claude/skills/sd-deploy/templates/git-hooks/post-commit`（auto-push.log + 失敗マーカー）
- `.git/hooks/post-commit`（実体・テンプレートと同期）
- `.claude/skills/sd-deploy/deploy.ps1`（gitignore Phase 5-5b拡張 / optional skills除外 / FRAMEWORK_VERSION 2.17.0）
- `.claude/skills/sd-deploy/deploy.sh`（gitignore処理を新規実装 / FRAMEWORK_VERSION 2.17.0）
- `.agents/skills/sd-deploy/` `.grok/skills/sd-deploy/`（AIミラー同期）
- `CLAUDE.md`（Grokモデル記述）
- `docs/troubleshooting/RESOLUTION_LOG.md`（事故2件を記録）

**他プロジェクト（各リポジトリでコミット済み）**
- 文字化け復元 229件 + 追加2件（18PJ）
- 版数刻印: ta001 / at002 / fl006 / ac001 の `CLAUDE.md`
- `.gitignore`: fw003 / oc001 / ac001
- 履歴書き換え: fw003 / oc001 / ac001

### 使用した外部ファイル

- `C:\AppData\Local\Temp\claude\D--claudecode-sd003\23de23e7-652f-446f-972a-5f6d993a4eec\scratchpad\restore_mojibake.py`
  （文字化け一括復元スクリプト。`--apply` で適用。Tier A/B 判定ロジック入り。**再利用価値あり**）
- `C:\AppData\Local\Temp\claude\...\scratchpad\commit_restores.py`
  （復元ファイルだけを選択stageしてcommitするスクリプト）
- `C:\AppData\Local\Temp\claude\...\scratchpad\ac001-precleanup-20260725\`
  （ac001 の破棄前バックアップ。files/ に41ファイル + MANIFEST.txt + untracked-moved/）
- `C:\AppData\Local\Temp\claude\...\scratchpad\ac001-CLAUDE.md.corrupted-20260725.bak`（破損版CLAUDE.md）
- `D:\claudecode\fw003\.git\pre-filter-backup-20260725.bundle`（238MB）
- `D:\claudecode\oc001\.git\pre-filter-backup-20260725.bundle`（389MB）
- `D:\claudecode\ac001\.git\pre-filter-backup-20260725.bundle`（272MB）
- Artifact（無音故障レポート）: https://claude.ai/code/artifact/0806e92e-a00a-4d29-92c8-f754d7c9fc6a
- Artifact（フリート監査レポート）: https://claude.ai/code/artifact/f5bae2a9-9794-41ce-9e49-f76057997c7a

### 次回タスク

#### P0（緊急）
なし

#### P1（重要）
1. 44PJの壊れた `agent-review.sh` をどうするか判断（`/sd-upgrade` 展開 or 最小配布）。
   放置するとコミットのたびに無意味なファイルが撒かれ続ける
2. pre-commit で50MB超ファイルをブロックする物理ガードレールを新設
   （100MB超commitは履歴書き換えまでpush恒久不能になるため、入口で止めるのが正解）
3. B17（auto-push可視化）を配信先へ展開する判断。現状44PJは無音pushのままで、
   今回のような「19コミット取り残し」が再発しても誰も気づけない

#### P2（通常）
1. バックアップbundle 3件（計899MB）の削除タイミング判断（安定確認後）
2. oc001 の切れたシンボリックリンク `.claude/skills/webapp-testing` を解消
3. nm002 の版数刻印を 2.15.0 に修正
4. deploy.sh に optional skills 除外を実装（ps1との機能差解消）
5. ac001 の `.claude/commands/` に SD001期の旧コマンド（ralph-wiggum-*, refactor-*, workflow-*）が
   残存。sd003では撤去済みの機構のため整理候補

### 備考

- **今日の教訓の核**: `2>/dev/null` は「失敗の無音化装置」。1秒で判明する引数エラーが4か月生き延びた原因はこれ1点。
  同様に旧 post-commit の `>/dev/null 2>&1 &` が19コミットの取り残しを隠していた。**エラーパスでstderrを捨てない**
- **一括処理は失敗も一括で配る**: 1つの移行スクリプトが18リポジトリを同時に破壊した。
  「1件で試す → 検証 → 残りへ展開」の段取りを必ず挟むこと
- **gitが唯一の復元元だった**: 229件すべて親リビジョンから復元できた。未コミットで一括処理を流していたら全損
- 過去に `24ef3aa`「BOM/mojibake fix WIP, 287 files」で修復が試みられ**途中で止まっていた**。
  WIPで止めた作業は残りが自動的に消えるわけではない
- ユーザー選択の記録: ①自動レビュー=opt-in維持 ②配信先展開=今はsd003のみ ③無音4件だけ手当て
  ④229件すべて復元 ⑤deploy.ps1の粗を直す ⑥クリーンな16PJだけcommit ⑦ac001は消えてもいいのでクリーン化

### 学習ナッジ（修正が2回以上ある場合のみ記載）
- 修正2回検出
- 修正内容:
  1. ac001 CLAUDE.md の文字化けを「cp932経由の復元も失敗、原文は機械的には戻せません」と報告 → ユーザー「直せ」
     → git履歴を遡れば移行直前のクリーン版が存在し、**完全復元できた**。
     復元不能と結論づける前に全リビジョンを走査していなかった
  2. ac001 を「未コミット99件が消えるため保留」と自己判断 → ユーザー「消えてもいいのでクリーンにしてくれ」
     → 保留にするかどうかは自分で決めず、選択肢としてユーザーに出すべきだった
- 永続化提案: auto-memory feedback 記録を検討
  （「復元不能」と断ずる前にgit全リビジョンを機械走査する / トレードオフのある保留判断は自分で決めずユーザーに委ねる）
