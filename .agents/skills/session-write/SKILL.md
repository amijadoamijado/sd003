---
name: session-write
description: Legacy alias for `sessionwrite`. Use when the user invokes `/sessionwrite` or `session-write` in Codex.
---

# session-write

この skill は `sessionwrite` の互換エイリアスです。
`sessionwrite` と同じ手順で `/sessionwrite` を再現してください。
