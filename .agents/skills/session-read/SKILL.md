---
name: session-read
description: Legacy alias for `sessionread` (SD003 command /sessionread).
disable-model-invocation: true
---

# session-read (alias)

This skill is an alias of `sessionread`. Follow the same steps as `sessionread` to reproduce `/sessionread`.

User-provided arguments (if any): $ARGUMENTS
