# セッション記録

## セッション情報
- **日時**: 2026-06-10 20:52:10
- **プロジェクト**: D:\claudecode\sd003（SD003フレームワーク本体）
- **ブランチ**: master
- **最新コミット**: 679fabc fix(hooks): L4 wipe-restore upgraded to snapshot-based file-level detection

## 作業サマリー

前回保存（20:29 ss001展開完遂）の直後、ユーザーの「.sd/ wipe 発生 これ改善できないのか？」を受けて
L4防御（git hooks）をスナップショット方式に強化した。文書化済みだった改善候補2件を実装。

### 完了

1. **L4 wipe防御のスナップショット方式強化（コミット `679fabc`）**
   - 旧L4の2つの限界（①partial wipe非検知=`[ ! -d .sd ]`判定、②HEAD復元のため未commit分喪失）を解消。
   - **pre-commit**: 自動ステージ後に `.sd/` 全体を `.git/sd-snapshot/` へ複製（`.git/`内はランタイムのworking tree refresh対象外で生き残る）。
   - **post-commit**: ディレクトリ全消失判定 → **スナップショット基準のファイル単位判定**。欠損ファイルのみ復元（残存ファイルのローカル編集は不可侵）。スナップショット不在時はHEADフォールバック（旧挙動）。
   - 意図的削除（commit前にディスクから消したファイル）はスナップショットに入らないため復活しない。

2. **実機テスト17ケース全PASS（temp使い捨てリポジトリ）**
   - full wipe全復元 / partial wipe欠損のみ復元 / 残存ファイル編集の不可侵 / no-wipe時の無反応 /
     意図的削除の非復活 / HEADフォールバック / commit時点の未commit相当ファイルの保護。
   - テストスクリプト: `C:\AppData\Local\Temp\sd003-staging\test-sd-hooks.sh`（使い捨て）。

3. **実弾実証2回（実装中に本物のwipeが2回発生）**
   - 1回目: コミット試行Bash自体がMSYS fatal errorで失敗 → **mid-session wipeで.sd/ 58ファイル消失** → 新post-commit手動実行（`bash .git/hooks/post-commit`）でHEADフォールバック全復元。
   - 2回目: コミット成功時（679fabc）にもwipe → HEADフォールバックで58ファイル自動復元。

4. **重要観察: wipeはpre-commit実行前に発火していた**
   - 679fabcのコミットで「no snapshot found」= pre-commit時点で既に.sd/が消えていた。
   - **wipeは「commit後のrefresh」ではなく「Bashツール起動時のrefresh」で発火している可能性が高い**（従来理解の精緻化）。
   - この場合でもpre-commitの早期exitで前回スナップショットが温存される設計（今回は初回のため不在→HEADフォールバックが機能）。

5. **正本・文書・メモリの同期**
   - deployテンプレート正本（`.claude/skills/sd-deploy/templates/git-hooks/`）を更新＝配信先には/sd-upgradeで届く。
   - sd003ローカル `.git/hooks/` へ導入＋`.git/sd-snapshot` を手動初期化（58ファイル、即時カバレッジ確保）。
   - `.claude/rules/git/sd-safe-commit.md`（L4強化・改善候補2件を実装済みに）、`docs/bug-workaround-sunset.md`（L4行更新）。
   - auto-memory `feedback_sd_directory_disappearance.md` 全面更新＋MEMORY.mdインデックス更新。

### 進行中
（なし）

### 未解決

1. **mid-session wipe（commit間）の自動復元なし**: sd-watchdogは警告のみ（意図的設計）。スナップショット復元型への拡張案はあるが**ユーザー判断待ち**。手動復元は `bash .git/hooks/post-commit` 一発。緩和策=早めcommit。
2. **配信先のhookは旧版のまま**: 本日upgradeしたss001/at002/nm002も旧hook。次回の残り配信先展開時にss001等への再展開（hooks更新）を合わせる。
3. **deploy.ps1 Phase 6のSkillsカウント誤報**（前セッションから継続、P2）。

### 作成・変更ファイル

**sd003本体（コミット 679fabc）**
- `.claude/skills/sd-deploy/templates/git-hooks/pre-commit`（スナップショット採取追加）
- `.claude/skills/sd-deploy/templates/git-hooks/post-commit`（ファイル単位検知・スナップショット復元化）
- `.claude/rules/git/sd-safe-commit.md`（L4強化反映・改善候補更新）
- `docs/bug-workaround-sunset.md`（L4行更新）

**git管理外**
- `.git/hooks/pre-commit` / `.git/hooks/post-commit`（テンプレから導入）
- `.git/sd-snapshot/`（手動初期化58ファイル）

**auto-memory**
- `~/.claude/projects/D--claudecode-sd003/memory/feedback_sd_directory_disappearance.md`（L4強化・wipe発火タイミング観察を反映）
- `MEMORY.md`（同エントリの1行更新）

### 使用した外部ファイル
- `C:\AppData\Local\Temp\sd003-staging\test-sd-hooks.sh`（hook実機テストスクリプト・使い捨て、temp gitリポジトリで実行）

### 次回タスク

#### P0（緊急）
（なし）

#### P1（重要）
1. 残りの現役配信先（oc001 / fw5yp / sb001 / er001 等）へ `/sd-upgrade` 展開。**その際ss001/at002/nm002へ新git hooks（679fabc）も再展開する**（upgrade再実行 or hooks 2ファイルのみcp）。

#### P2（通常）
1. sd-watchdogのスナップショット復元型拡張（mid-session wipe対応）— ユーザー判断を仰いでから。
2. deploy.ps1 Phase 6カウント修正（Skills期待値のoptional除外反映、118/119型誤報根絶）。
3. deploy共通化 Stage1（generate-framework-files.mjs）/ メモリ逼迫恒久対策 / セッションアーカイブ（at002 16件/39MB）。

### 備考

- **wipe発火タイミングの新観察は要追跡**: 「Bashツール起動時のrefresh」説が正しければ、wipeはcommitと無関係に任意のBash呼び出しで起こり得る（実際mid-session wipeを1回実測）。sd-watchdog拡張の判断材料になる。
- **MSYS fatal error（`add_item failed, errno 1`）でBashが落ちることがある**: exit 5・無出力。リトライで成功した。wipeと同時発生しており関連の可能性あり。
- **改修はWork First順序で実施**: 既存hook読解→テンプレ書き換え→temp repoで17ケース実機テスト→ローカル導入→実弾で2回検証→文書・メモリ同期、の順。

### 学習ナッジ（修正が2回以上ある場合のみ記載）
- 修正0回検出（提案なし）
