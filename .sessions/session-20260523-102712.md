# セッション記録

## セッション情報
- **日時**: 2026-05-23 10:27:12
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: a7ce92e feat(sd-upgrade): new /sd-upgrade skill for safe framework replacement

## 作業サマリー

### 完了

1. **agy スラッシュコマンド不具合の根本原因究明（実機検証）**
   - 症状: agy 起動でスラッシュコマンドが全く出ない
   - 根本原因: 前回の gemini→agy 移行が `.gemini/commands/*.toml` パターンを `.antigravity/commands/*.toml` に機械的コピーしただけ。**agy は TOML を読まず、Agent Skills（`.agents/skills/{name}/SKILL.md`）でコマンドを読む**仕様
   - 実機検証で確定: 正しいパスに SKILL.md を置いたら `/skills` が 0→57→60 に変化。診断スキルを agy に実行させ、ディスク副作用（`AGY_LOADED_SKILL_MD_OK`）を自分で確認
   - **agy-agent の自己報告は虚偽だった**（A/Bテストで「復元した」と報告したが .bak が未復元）。報告に押されて一度結論を撤回したが、実機ディスク証明で当初診断が正しいと再確定

2. **agy コマンド機構の修正本体**
   - `sync-cli-commands.py` を TOML出力 → `.agents/skills/{slug}/SKILL.md` 生成に改修（実スキルも `.claude/skills/*` から `.agents/skills/` へミラー）
   - description のコロン（`スキルインストール: /skills:add`）で YAML が壊れ 3スキルが skip された件をクォートで修正
   - 誤生成物 `.antigravity/commands` `.antigravity/skills` を撤去

3. **総合監査（subagent）→ 不備一掃**（ユーザー指示「総合的にチェックして不備を修正」）
   - deploy.ps1/sh: gemini/.antigravity 撤去 → `.agents/skills` 伝播 + verify、死参照 sync-gemini-features.js 除去、gemini.md生成→antigravity.md配備
   - hook `workflow-state-tracker.sh`: 検出を `gemini`→`agy`（壊れていた impl完了ゲート復旧）
   - doctrine drift: coverage80% を agent-implement/pipeline/antigravity.md/AGENTS.md/テンプレで VTD+実データ に置換
   - docs全面書換: README/AGENTS/antigravity/sd-deploy を agy 化、Gemini/Cursor/Windsurf 撤去
   - `.antigravity/rules.md` 削除、`/CLAUDE` ジャンクskill撲滅、cosmetics（clean win32対応・migrate-kiro DEPRECATED・git-worktrees agy化）

4. **新規 `/sd-upgrade` スキル作成**（ユーザー要望）
   - 古いSD003 install を**プロジェクト資産を壊さず**最新へ置換（detect→dry-run→backup→deploy→verify）
   - DELETE list（廃止物）と PROTECT allowlist（src/.sd/specs/.sessions履歴/materials/.clasp.json/.git/.agents/skills）
   - throwaway で実機テスト合格（保護資産無傷・廃止物退避・.agents/skills(60)+myskill保存・stub混入なし）

5. **claude-mem 完全アンインストール**（非公式third-party @thedotmack と確認・ユーザー承認）
   - npm global 削除（74パッケージ）、プラグイン本体（claude-mem@thedotmack v8.2.9）+ marketplace 2件（thedotmack/claude-mem-jp）+ キャッシュ削除
   - スタブ13件削除、sync除外 + .gitignore（再汚染防止）
   - 残存は `~/.claude.json` の githubRepoPaths キャッシュキー1件のみ（無害・温存）

### 進行中
なし

### 未解決
1. **claude-mem 反映待ち**: Claude Code 再起動でプラグイン無効化が反映（スタブ再生成停止）。現セッションは起動時読込済み
2. **agy 最終確認待ち**: agy 再起動 → `/skills` に `/sd-upgrade` 表示・警告0件 をユーザー確認
3. **下流プロジェクト（at002/oc001等）への展開**: 今回スコープ外。`/sd-upgrade <path>` で各PJ最新化可能
4. **`.sd/commands/specs` の追跡化**: pre-commit hook が force-stage している（.sd は本来gitignore）。post-commit hook が毎回 .sd を復元。挙動は安定だが設計的整理は別途検討余地

### 作成・変更ファイル

**新規**:
- `.claude/skills/sd-upgrade/{SKILL.md, upgrade.ps1, upgrade.sh}`（+ `.agents/skills/sd-upgrade/` ミラー）

**変更（主要）**:
- `scripts/sync-cli-commands.py`（SKILL.md/.agents生成・CLAUDE.mdスキップ・YAMLクォート・ミラー時CLAUDE.md除外）
- `.claude/skills/sd-deploy/{deploy.ps1, deploy.sh, SKILL.md, README.md}`、templates（gemini/antigravity-rules削除、coverage修正）
- `README.md`, `AGENTS.md`, `antigravity.md`, `.codex/CODEX_SPEC.md`, `package.json`, `.gitignore`
- `.claude/hooks/workflow-state-tracker.sh`, `scripts/agent-implement.sh`, `scripts/agent-pipeline.sh`
- `.claude/skills/git-worktrees/SKILL.md`, `scripts/migrate-kiro-to-sd.ps1`

**削除**:
- `.antigravity/`（commands/skills/rules.md 全て）、`.gemini`系テンプレ2件、claude-mem スタブ13件

**グローバル（git管理外）**:
- `~/.claude/plugins/installed_plugins.json`, `known_marketplaces.json`（claude-mem除去、.bak バックアップ有）
- npm global から claude-mem 削除

### 使用した外部ファイル
- なし（プロジェクト外ファイルの入力使用なし。throwaway は `D:\claudecode\_sdupg_test` に一時作成→削除済み）

### 次回タスク

#### P0（緊急）
なし

#### P1（重要）
1. **Claude Code 再起動 + agy 再起動で最終確認**（claude-mem無効化反映 / `/skills` に `/sd-upgrade` 表示・警告0件）
2. **下流プロジェクトへの展開**: `/sd-upgrade <path>` で at002/oc001 等を最新化（1PJずつ dry-run→確認→execute）

#### P2（通常）
1. `.gitignore` 末尾の `# test-N` コメント残骸の整理（純コスメ・今回温存）
2. `.sd/commands/specs` の追跡化（pre-commit force-stage）の設計整理を検討
3. sd-deploy/README.md は冒頭にレガシーバナーを付けたのみ。全面書換は別途（低優先）

### 備考

- **本セッションのコミット10件**（`7e7b314`〜`a7ce92e`）。ビルド(tsc) exit 0 / sync --check OK(35) / .agents SKILL.md 60件 YAML invalid=0 / アクティブ箇所の旧gemini参照ゼロ
- **agy機構の知見を auto-memory に永続化**: `reference_agy_command_mechanism`, `feedback_agy_self_reports_unreliable`, `reference_claude_mem_unofficial`（MEMORY.md更新済み）
- **CRLF警告**は git autocrlf の整形通知で無害
- **plan承認後に実装**: jolly-herding-wreath.md（計画ファイル）に沿って Group A〜I を順次実施

### 学習ナッジ（修正が2回以上ある場合のみ記載）
- 修正2回検出（いずれもプロセス上の重要修正）
- 修正内容:
  1. agy-agent の虚偽報告に押されて正しい結論（SKILL.md機構）を一度撤回 → 実機ディスク証明で再確定。「報告 < 実機の事実」を徹底すべき（→ `feedback_agy_self_reports_unreliable` に永続化済み）
  2. print mode で agy スラッシュコマンドを検証しようとして失敗（print modeは無視する仕様）→ 対話モード/ディスク副作用での検証に切替。検証手段の妥当性を先に確認すべき
- 永続化提案: 上記1は auto-memory feedback に記録済み。追加ルール化は不要（既存の root-cause-first / Work First でカバー）
