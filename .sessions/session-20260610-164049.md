# セッション記録

## セッション情報
- **日時**: 2026-06-10 16:40:49
- **プロジェクト**: D:\claudecode\sd003（SD003フレームワーク本体）
- **ブランチ**: master
- **最新コミット**: 984e390 session: 2026-06-10 SD003評価+推奨5件全改修

## 作業サマリー

本セッションは前回P1「修正の配信先展開」を at002 / nm002 の2件に絞って完遂した（ユーザー指定スコープ）。
sd003本体のコード変更はなし。変更は全て配信先（at002 / nm002）側のリポジトリに着地。

### 完了

1. **at002へ /sd-upgrade 展開（at002コミット `8c57d7d`）**
   - dry-runで33固有化divergenceを検出 → 差分規模で仕分け（CLAUDE.md 298行差・settings.json 39行差・hooks 403行差＝06-10セッションの意図的固有化）。
   - `.sd003-keep` を新規作成し固有5ファイル（CLAUDE.md / settings.json / hooks 3本）を保護、残りFW 29ファイルをv3.2.0で上書き。430コピー+7生成、ALL PASSED。
   - 配付先テスト `verify-deployment.mjs` C1〜C6全PASS（17 hooks配線健全＝settings.json keepでも手動マージ不要と判明）。
   - `.gitignore` に `.sd003-upgrade-backup-*/` 欠落を発見し追加。ブランチ `claude/phase1-ocr-knowhow` に266ファイルコミット。

2. **nm002のゴミファイル803個削除（ユーザー許可済み）**
   - ルート直下の8文字ランダム名ファイル801個（+漏れ2個）が全て4バイト「blat」の汚染（2026-05-05作成、計3MB）と特定。
   - 二重ガード（サイズ4バイト＋内容blat＋git未追跡）で削除。`_archive`（正規ディレクトリ）は正しくスキップ。

3. **nm002のP1.2＋P1.1統合完遂（nm002コミット `42e9b38`, `a56174c`）**
   - 06-07の未コミット /sd-upgrade 残骸とFW更新が混在 → `.gitignore` にSD003成果物除外（backup/tmp/worktrees等）を追加してから、06-07分＋今日のv3.2.0更新を1コミットに統合（383ファイル）。
   - product資産（dist/nm002-reconcile.tar.gz、docs 2件、要件docx）は意図的に除外・温存。
   - dry-runで20 divergedが全てFWファイルでproduct非破壊と確認してからexecute。CLAUDE.mdはkeep保護。
   - 配付先テスト C1〜C6全PASS。.sd/ wipeなし（592ファイル健在）。

4. **nm002固有 pre-commit hookのバグ発見・修正（根本対応）**
   - `scan-utf8-replacement.sh` がstdin mode（`git show :f | scanner`）でallow-list照合を完全バイパスする欠陥を特定（allow-list追記だけでは直らないことを実測で確認）。
   - `--stdin-path <path>` モードを追加しwrapper（.git/hooks/pre-commit）からパスを渡すよう修正。4ケース単体テスト全PASS後、実コミット通過で実証。
   - `utf8-replacement-allow.txt` に `scripts/verify-deployment.mjs` を登録（U+FFFDリテラルはC5文字化け検出プローブ＝意図的、修正禁止）。

5. **C:ドライブ満杯（0GB）の緊急対応と真因特定**
   - at002 upgrade実行中に「No space left on device」発生。OCCキャッシュ1.27GB削除（グローバル設定の事前承認＋ユーザー即時指示、Frontier21非起動確認済み）＋Office診断テレメトリ0.41GB削除。
   - 小掃除は常駐書き込みに食い潰され無意味と実測。真因= `AppData\Roaming\Claude\vm_bundles` **12.5GB**（Claude Desktop VM bundle）。
   - F:退避手順（Move-Item＋mklink /J）を提示。Desktop終了が必要なためユーザー実行待ち。

6. **auto-memory 2件保存**
   - `project_c_drive_vm_bundles.md`（C:危機の内訳と退避手順）
   - `reference_verify_deployment_ufffd.md`（U+FFFDプローブの意図とhookバグ修正の知見）

### 進行中
（なし）

### 未解決

1. **C:ドライブ危機（最重要・ユーザー操作待ち）**: 空き約1GBで常時減少中。vm_bundles 12.5GBのF:退避はClaude Desktop完全終了が必要（手順はauto-memoryに保存済み）。
2. **nm002のproduct差分3件**: dist/nm002-reconcile.tar.gz・docs/quality-gates.md・docs/troubleshooting/RESOLUTION_LOG.md が未コミットのまま（FW外のためnm002側の業務判断待ち）。.sd/specs/mistake-report/ の要件docxも未追跡のまま。
3. **セッションアーカイブ未実行**: at002の13件/31MBが対象（`/archive-sessions --execute` で実行可能）。

### 作成・変更ファイル

**sd003本体**: なし（セッション記録のみ）

**at002側（コミット 8c57d7d）**
- `.sd003-keep`（新規・固有5ファイル保護）
- `.gitignore`（upgrade-backup除外追加）
- FW 29ファイル更新＋新規213ファイル（ワークフロー土台・.agents/skills 62件等）

**nm002側（コミット 42e9b38, a56174c）**
- `.gitignore`（SD003成果物除外セクション追加）
- `.claude/hooks/scan-utf8-replacement.sh`（--stdin-pathモード追加）
- `.claude/hooks/utf8-replacement-allow.txt`（verify-deployment.mjs登録）
- `.git/hooks/pre-commit`（scanner呼び出しにパス渡し・git管理外）
- FW v3.2.0更新一式＋06-07未コミット分（383ファイル）＋ verify-deployment.mjs

**auto-memory**
- `~/.claude/projects/D--claudecode-sd003/memory/project_c_drive_vm_bundles.md`（新規）
- `~/.claude/projects/D--claudecode-sd003/memory/reference_verify_deployment_ufffd.md`（新規）
- `MEMORY.md` 更新

### 使用した外部ファイル
- `D:\claudecode\at002\`（upgrade対象。.sd003-keep作成・.gitignore編集・コミット 8c57d7d）
- `D:\claudecode\nm002\`（upgrade対象。ゴミ削除・hook修正・コミット 42e9b38, a56174c）
- `C:\Users\a-odajima\AppData\Roaming\OCC\F21\...\0004d8cfグループ1\`（OCCキャッシュ1.27GB削除）
- `C:\AppData\Local\Temp\Diagnostics\`（Office診断テレメトリ0.41GB削除）

### 次回タスク

#### P0（緊急）
1. **vm_bundles 12.5GBのF:退避**（ユーザーがClaude Desktop終了後に実行。手順はauto-memory `project_c_drive_vm_bundles.md`）。完了までsd-upgrade等の大量コピー作業はC:満杯リスクあり。

#### P1（重要）
1. 残りの現役配信先（oc001 / fw5yp / sb001 / er001 等）へ `/sd-upgrade` 展開（at002/nm002と同じ手順: dry-run→keep仕分け→execute→verify。**C:空き確保後に実施**）。

#### P2（通常）
1. deploy共通化 Stage1: Phase5（Generate）のNode統合 `scripts/generate-framework-files.mjs`。
2. マシンメモリ逼迫の恒久対策（SQL Server常駐見直し。05-27からの継続課題）。
3. セッションアーカイブ実行（at002 13件/31MB）。

### 備考

- **upgrade前の現状確認が効いた**: nm002は「いきなりexecute」せず先にgit statusを見たことで、801個のblat汚染と06-07未コミット分の混在を発見できた。「ゴミ掃除→過去分コミット→今日のupgrade」の順序で差分の混濁を回避。
- **dry-runのdivergence仕分け基準**: 差分行数で「意図的固有化（keep）」と「古いFW（上書き）」を客観判定できた（at002: CLAUDE.md 298行差=固有、spec-versioning 4行差=古いFW）。
- **回避策よりhook修正**: verify-deployment.mjsのコミットブロックに対し「ファイル除外」の回避策をユーザーが拒否 → root-cause-firstでhook自体のstdin-modeバグを修正。結果、同型スキャナを持つ他プロジェクトにも効く恒久知見になった。
- **ディスク満杯時の挙動**: C:0GBでは `head` などの基本コマンドまで "No space left on device" で失敗する。upgrade.shはD:対象でもC:のTEMP書き込みで死ぬ。

### 学習ナッジ（修正が2回以上ある場合のみ記載）
- 修正1回検出（2回未満のため提案なし）
- 修正内容（参考）: verify-deployment.mjsをコミットから除外する回避策をユーザーが拒否 → hookバグの根本修正に切り替え（root-cause-first適用）。
