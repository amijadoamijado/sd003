# セッション記録

## セッション情報
- **日時**: 2026-04-17 12:51:52
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 9c3d1ed session: SD003 Core Doctrine 4本柱制定 + ガードレール Phase A-C（思想+テンプレート+コマンド強制）+ Playwright共有化

## 作業サマリー

### 完了
1. **TOMLパースエラーの解消**: `sd-deploy.toml` や `sessionread.toml` で発生していたバックスラッシュのエスケープ不備によるパースエラーを解消しました。
2. **同期スクリプトの統一**: Python (`sync-cli-commands.py`) と JS (`sync-gemini-features.js`) に分散していた同期ロジックを Python スクリプトに一本化しました。
3. **コマンド・スキルの競合解消**: スキルとして定義されている機能（`sd-deploy`, `blueprint-gate` 等）がコマンドとしても生成され、Gemini CLI で競合警告が出ていた問題を、スキル優先で生成をスキップするロジックを導入して解決しました。
4. **スキルディレクトリの整理**: ジャンクション（シンボリックリンク）になっていた `.claude/skills/` 配下のディレクトリを実ディレクトリに変換し、`.agents/` などの冗長なディレクトリを削除しました。
5. **Codex スキル配置の変更**: Gemini CLI がスキャンしないよう、Codex 用のスキルを `.codex/skills` に配置するように変更しました。

### 進行中
- なし

### 未解決
- なし

### 作成・変更ファイル
- **scripts/**: `sync-cli-commands.py` (大幅改修), `sync-gemini-features.js` (削除)
- **package.json**: `sync:gemini` スクリプトの更新
- **.claude/skills/**: ジャンクションから実ディレクトリへの変換
- **.gemini/commands/**: TOMLファイルの再生成
- **.gemini/skills/**: スキルファイルの同期
- **.codex/skills/**: Codex用スキルの配置

## 次回タスク

#### P0（緊急）
- なし

#### P1（重要）
- 新しい同期フローの安定性確認
- 必要に応じて `.geminiignore` の見直し

#### P2（通常）
- 他の環境（Linux等）での同期スクリプトの動作確認

### 備考
今回の修正により、Gemini CLI 起動時の警告が消え、設定ファイル（TOML）のパースも正常に行われるようになりました。また、スキルとコマンドの管理が Python スクリプト一つで完結するようになり、保守性が向上しました。

### 学習ナッジ
- 修正2回以上検出（TOMLエスケープ関連、ディレクトリ構成関連）
- 修正内容:
  1. TOMLの prompt セクションを基本文字列 (`"""`) からリテラル文字列 (`'''`) に変更し、エスケープ問題を根本解決。
  2. スキルとコマンドの重複を `CLAUDE_SKILLS_DIR` の存在チェックで自動回避。
- 永続化提案: `sync-cli-commands.py` を正本管理の唯一の手段として維持する。
