# セッション記録

## セッション情報
- **日時**: 2026-09-19 16:34:16
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 4a70746 session: resolve aa001 remote, deploy git-repo guard, deploy.sh settings.json parity
- **Lead**: Grok（lock 取得済み）

## 作業サマリー

### 完了

1. **Grok の PreToolUse / PostToolUse が毎ツール `exit code 1` になる原因を特定した**
   - このセッション開始時点で session_start 2本失敗、以降 `run_terminal_command` ごとに project PreToolUse が5〜6本失敗。fail-open のためツール自体は動くが、画面上は失敗が積み上がり、bash 製ガードは実質死んでいた
   - `grok inspect` でソースは `vendor: claude` / `D:/claudecode/sd003/.claude`。`compat.claude.hooks` 既定 true で `.claude/settings.json` を取り込み、`bash "$CLAUDE_PROJECT_DIR/.claude/hooks/..."` を実行していた
   - PATH 先頭の `bash` は `C:\WINDOWS\system32\bash.exe`（WSL スタブ、86016 bytes）。ディストリは未導入。`wsl -l` はインストール案内で終わる
   - `node scripts/orchestrator-guard.js` だけ成功（node は PATH にある）。同じスクリプトを Git Bash `d:\Program Files\Git\bin\bash.exe` に渡すと exit 0

2. **WSL は入れずに Windows だけで直した**（ユーザー明示: 入れる予定なし）
   - 新規 `scripts/run-hook.js`: System32/SysWOW64 の WSL スタブを使わず、`CLAUDE_CODE_GIT_BASH_PATH` → Git 定位置 → `git.exe` 隣接 `bin\bash.exe` の順で本物を選ぶ。Grok の `toolInput` を Claude フックが読む `tool_input` に正規化して stdin へ渡す
   - `.claude/settings.json`（gitignore・実行時実体）と deploy 正本 `settings.json.template` の全 `bash "...hooks/*.sh"` を `node ".../run-hook.js" "...hooks/*.sh"` に置換。`orchestrator-guard.js` はそのまま
   - `deploy.ps1` / `deploy.sh` が `run-hook.js` をコピー。`verify-deployment.mjs` に C2a（ランチャー同期）を追加

3. **User PATH の穴を塞いだ**
   - User/Machine PATH には `d:\Program Files\Git\cmd`（git.exe）だけがあり、`Git\bin`（bash.exe）が無かった
   - User PATH 先頭に `d:\Program Files\Git\bin` を追加。プロセス内 `where bash` の先頭は Git Bash（OSTYPE=msys）になった。System32 スタブは後ろに残るが選ばれない

4. **ランチャー単体の実測**
   - session-skill-suggest / prune-skill-state / sd-watchdog → exit 0
   - Grok 形式 payload（camelCase `toolInput` のみ）で clasp undeploy → deny JSON（ガード生存）
   - `verify-deployment.mjs` を sd003 自身に対して実行 → C1〜C8 全 PASS（C2a 含む）

### 進行中

なし（本セッションの調査・修正は完了。効果の最終確認は Grok 再起動後）

### 未解決

1. **この Grok 窓は起動時の PATH とフック一覧のまま**。settings.json は既に `run-hook.js` 経由だが、セッション開始時に読んだ旧 `bash ...` コマンドがメモリに残っており、この窓では project hook がまだ exit 1。**Grok を終了して新しい窓で開き直す必要がある**。開き直し後の成功は未確認
2. 前回持ち越し: aa001 の `npm install` / gas-fakes 要否（別セッション）
3. 前回持ち越し: `D:\claudecode\aa001\.sd003-backup-20260916_080708`（空フォルダ）残置
4. 前回持ち越し: 既存デプロイ先への Phase 1b/1c 伝播は `/sd-upgrade` 待ち

### 作成・変更ファイル

- `scripts/run-hook.js`（新規）— Windows で WSL スタブを使わないフック起動
- `.claude/settings.json`（gitignore・実行時配線。git には乗らない）
- `.claude/skills/sd-deploy/templates/settings.json.template` — 正本
- `.claude/skills/sd-deploy/deploy.ps1` / `deploy.sh` — `run-hook.js` 配布
- `scripts/verify-deployment.mjs` — C2a
- 本ファイル、`.sessions/session-current.md`、`.sessions/TIMELINE.md`、`.handoff/DONE.md`

### 使用した外部ファイル

- `C:\Users\a-odajima\.grok\hooks\orca-status.json`（Orca の Grok PreToolUse。こちらは成功していた）
- `C:\Users\a-odajima\.orca\agent-hooks\grok-hook.cmd`（Orca 通知。curl 失敗は無視して exit 0）
- `C:\WINDOWS\system32\bash.exe`（WSL スタブ。未導入時 exit 1 の実体）
- `d:\Program Files\Git\bin\bash.exe`（本物。CLAUDE_CODE_GIT_BASH_PATH と同じ）
- `D:\grok\docs\user-guide\10-hooks.md` / `05-configuration.md`（compat.claude.hooks 既定 true、CLAUDE_PROJECT_DIR 注入）
- `D:\grok\sessions\D%3A%5Cclaudecode%5Csd003\01a0b87e-1a1e-7d10-af78-741159a3a702\updates.jsonl`（本セッションの hook_execution 失敗ログ）
- User PATH（永続変更: `d:\Program Files\Git\bin` を先頭追加。git 対象外）

### 次回タスク

#### P0（緊急）
- **Grok を一度終了し、新しいセッションで開く**。`run_terminal_command` の project PreToolUse が失敗ゼロであること、session_start 2本が成功することを `updates.jsonl` で確認する

#### P1（重要）
- 既存デプロイ先へ `run-hook.js` 配線を渡すには `/sd-upgrade`（ブートストラップ制約）。Windows で Grok を使う配布先は同じ WSL スタブ問題を踏む
- aa001 の `npm install` / gas-fakes 要否（実装方針待ち）

#### P2（通常）
- `D:\claudecode\aa001\.sd003-backup-20260916_080708`（空）の整理方針
- 本セッションのコード変更は sessionwrite と同時に commit する想定

### 備考

- WSL はインストールしていないし、入れない。Windows の Git for Windows（MSYS bash）で足りる
- Claude Code は元から `CLAUDE_CODE_GIT_BASH_PATH` で Git Bash を使うため、同じ settings.json 変更は Claude 側でも透過（node → Git Bash）
- ガードを切る案（`compat.claude.hooks = false`）は不採用。ノイズは消えるが clasp / `.sd/` 破壊禁止が Grok で死ぬ
- この窓での失敗表示は「修正が効いていない」証拠ではなく、起動時キャッシュの残り

### 学習ナッジ（修正2回）
- 修正2回検出
- 修正内容:
  1. 「WSL は設定していないぞ」→ スタブの説明は正しいが、入れていない前提を先に固定すべきだった
  2. 「入れる予定ない。Windows環境で解決してくれ」→ ランチャーだけでなく User PATH の `Git\bin` 欠落まで直す
- 永続化提案: Windows で `bash` は System32 の WSL スタブを拾う、Git は `cmd` だけ PATH に乗り `bin` は乗らない、を auto-memory または規範1行に残す検討
