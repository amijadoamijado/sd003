# セッション記録

## セッション情報
- **日時**: 2026-06-07 16:40:06
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 9f0455d fix(deploy): per-event hook wiring check (C1), template-specific placeholders (C3), include .ps1 in mojibake scan (C5)

## 作業サマリー

### 完了

1. **プロンプト/Skill自動最適化技術（GEPA・SkillOpt・DSPy）のブリーフィング文書を批判的レビュー**
   - 「JEPAR」は誤記で正しくは **GEPA**（arXiv 2507.19457, ICLR 2026 Oral）であることをWeb一次情報で確定。説明内容（木構造探索/パレート支配/D-Pareto検証/GRPOを最大35倍少ロールアウトで凌駕）はGEPAと完全一致。
   - **SkillOpt** は実在（Microsoft, arXiv 2605.23904, 2026-05）。3機構（学習率上限/棄却バッファ/メタ最適化）も記載通り正確。Claude Code内で +19.1点の実測あり。
   - **DSPy** は `dspy.GEPA` として実装可能。結論: SD003のcheck-*群がルブリック資産＝GEPA/SkillOptの素地。

2. **SD003フレームワーク自体を自ドクトリン（柱1-4）で評価** → ユーザー訂正で物差しを是正（後述・学習ナッジ）。

3. **修正案: sd-deploy にデプロイ時スモークテスト（内容検証ゲート Phase 6b）を実装**
   - 計画→承認→実装→実機検証→コミットまで完遂。
   - 根本原因2点を特定: ①既存Phase 6はファイル数・存在しか見ず settings.json の中身を検証しない（9f14984のStop-only配線を素通り）。②deploy.ps1/sh は検証失敗でも `exit 0` で成功扱い（検証が飾り）。
   - 単一Node検証 `scripts/verify-deployment.mjs`（C1 hook配線/C2 hook実在/C3 未置換プレースホルダ/C4 廃止語/C5 文字化け/C6 JSON妥当性）。テンプレから期待値を導出する自己保守型。
   - deploy.ps1/sh に Phase 6b 配線＋失敗で **exit 1**（ハードフェイル）＋ verifier を配信先へもコピー。
   - 回帰テスト `tests/deploy/verify-deployment.test.ts` ＋ 9f14984再現fixture（Stop-only）。2件PASS。
   - SKILL.md / README.md にPhase 6b文書追記。コミット `7becd7c`。

4. **コードレビュー指摘2点＋自己発見1点を根本修正**（コミット `9f0455d`）
   - [P2] C1をファイル名集合の全体比較 → **イベント単位照合**へ（誤イベント配線を検出）。
   - [P3] C5の文字化け検査に **.ps1 を追加**（Windows用 sd003-stop-hook.ps1 等も対象）。
   - [自己発見] C3が `{{` を全部NGにして `{{NOW_CONTEXT}}`（正当なOCR変数記述）を誤検知 → テンプレ実プレースホルダ（{{DATE}}/{{ISO_DATE}}/{{PROJECT_NAME}}）限定に修正。

5. **現役プロジェクト at002 の健全性チェック＆修復**
   - ゲートが at002 の実害を捕捉: settings.json が古く SessionStart欠落＋hook9個未配線（特に `block-sd-destructive.sh` 未配線 = .sd/破壊防止が無効だった）。
   - 修復: 旧settings.jsonをバックアップ → 最新テンプレをコピー（deploy Phase 5-5と同一処理、settings.jsonのみ）→ 再検証で **C1-C6全PASS / exit 0**。

### 進行中
（なし）

### 未解決

1. **他の現役配信先の棚卸し未実施**: at001はat002へ移行済みで対象外（ユーザー確認済）。oc001/fw5yp/sb001/er001等の現役配信先は未チェック。`node scripts/verify-deployment.mjs <target> D:\claudecode\sd003` で随時可能。
2. **deployに既存壊れファイルの再生成手段がない件**: settings.jsonは上書き(-Force)するが、CLAUDE.md等のSKIP対象が壊れていても直せない。`--force-settings`相当は今回スコープ外。

### 作成・変更ファイル

**新規**
- `scripts/verify-deployment.mjs`（内容検証ゲート本体）
- `tests/deploy/verify-deployment.test.ts`（回帰テスト）
- `tests/fixtures/deploy-broken-settings/.claude/settings.json`（9f14984再現fixture）

**変更**
- `.claude/skills/sd-deploy/deploy.ps1`（Phase 6b追加・scripts/scan配列・末尾exit 1）
- `.claude/skills/sd-deploy/deploy.sh`（同上）
- `.claude/skills/sd-deploy/SKILL.md`（Phase 6b文書）
- `.claude/skills/sd-deploy/README.md`（Step 9b文書）

**at002側（sd003リポジトリ外・gitignore対象）**
- `D:\claudecode\at002\.claude\settings.json`（最新テンプレで上書き＝全hook再配線）
- `D:\claudecode\at002\.sd003-backup-settings-20260607_161951\settings.json`（旧版バックアップ）

### 使用した外部ファイル
- `D:\claudecode\at002\.claude\settings.json`（健全性チェック対象・修復対象）
- `D:\claudecode\at001\.claude\settings.json`（チェック対象。移行済で対象外と判明）

### 次回タスク

#### P0（緊急）
（なし）

#### P1（重要）
1. 現役配信先（oc001/fw5yp/sb001/er001等）の verifier 棚卸し。FAIL先は settings.json をバックアップ→最新テンプレ上書きで修復。

#### P2（通常）
1. deploy に `--force-settings` 相当（既存の壊れた生成物を再生成するモード）の検討。
2. verifier C4 deny-list の拡充検討（`/sd:spec-init` 等のstale ref候補。誤検知に注意）。

### 備考

- **本件の意義**: 「配送への Work First 適用」。ゲートは導入初日に現役プロジェクト at002 のガードレール不活性という実害を捕捉し、修復まで到達した。柱1（Output Primacy）をハーネス自身に正しく適用した成果。
- **deploy仕様の事実訂正**: 探索エージェントは「settings.jsonはSKIP(既存時)」と報告したが、実際は deploy.ps1:597 で `Copy-Item -Force` 上書き（テンプレ逐語コピー、OS変換・置換なし）。この事実により at002修復が settings.json のみのコピーで完結した。
- **.sd/消失とL4の実動作**: 本セッションのコミット2回とも post-commit hook が「.sd/ disappeared → restored from HEAD (42 files)」を実行。メモリ記載の通りL4が実防衛線として毎commit動作している実例。

### 学習ナッジ（修正が2回以上）
- 修正2回検出
- 修正内容:
  1. **ハーネスへの柱1誤適用**: SD003フレームワーク本体を `materials/` の成果物数で減点評価したが、ユーザー指摘「成果物はないよ、これはハーネスだから別プロジェクトに適用するもの」。フレームワーク/ライブラリは柱1の「画面必須」例外であり、正しい物差しは「適用先で即・動くか（検証済みか）」。→ この気づきが Phase 6b（配送のWork First）の設計動機になった。
  2. **検証ロジックの過剰検出（C3/C1/C5）**: 自作 verifier が `{{` を全部NGにして正当な記述を誤検知（C4で自分が警戒した過ちを別箇所で再発）。加えてレビューでC1（イベント単位照合の欠如）・C5（.ps1未検査）を指摘。実データ（現役at002）で走らせて初めて誤検知が露見した＝モックでなく実データで検証する原則の有効性を再確認。
- 永続化提案: 「ガードレール/検証コードは実データで発火試験するまで完成としない」「ハーネスの完了判定は適用先での動作」を auto-memory feedback として記録検討。
