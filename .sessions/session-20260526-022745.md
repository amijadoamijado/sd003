# セッション記録

## セッション情報
- **日時**: 2026-05-26 02:27:45
- **プロジェクト**: D:\claudecode\sd003
- **ブランチ**: master
- **最新コミット**: 93a6224 feat(codex-dispatch): bring hardened codex-dispatch into sd003 framework (was ungoverned global skill)

## 作業サマリー

### 完了

1. **sd-upgrade/deploy の固有化ファイル上書き欠陥を修正（commit 14230fd）**
   - 契機: 前セッションの at002 への `/sd-upgrade` が「UPGRADE OK / 保護無傷」と誤報しつつ、at002 の固有化FWファイル（CLAUDE.md / rules / hooks / registry.json / package.json）を汎用テンプレで踏み潰していた（ユーザー指摘「事故を起こしている」）
   - 根本原因（コードで確認）: `upgrade.ps1` は配備を `deploy.ps1` に委譲。`deploy.ps1` は「FWファイル=FW所有物→常に上書き」設計で、意図的な固有化を区別できない。upgrade の検証は `.agents/skills` 有無しか見ず、固有化の上書きを検知できないため誤報
   - 採用方針（ユーザー選択）: **正直化＋オプトアウト**
   - 実装:
     - `.sd003-keep` マニフェスト（オプトアウト）: 列挙したFWファイルを deploy/upgrade が skip。ファイル無しなら全ガード no-op（通常PJは挙動不変）
     - `-DryRun`/`--dry-run` divergence スキャン: 「WILL OVERWRITE - LOCAL CUSTOMIZATION WILL BE LOST」「KEPT via .sd003-keep」を事前一覧
     - execute 時に「OVERWROTE local divergence (backed up)」をファイル単位で報告
     - 判定文言を `UPGRADE OK` → `UPGRADE COMPLETE` ＋ 上書き警告へのポインタ
     - package.json も `.sd003-keep` 保護対象化（ConvertTo-Json 整形崩れ回避）
   - 検証: throwaway で PowerShell・bash 両方を end-to-end 検証（kept は固有化内容保持、非kept divergence は上書き＋バックアップ＋報告、復元可能）。SYNC CHECK OK、`.agents/skills` ミラー同期、throwaway 後始末済み
   - ドキュメント: sd-deploy / sd-upgrade の SKILL.md に `.sd003-keep` と正直化 dry-run を明記

2. **codex-dispatch を sd003 framework スキルとして正準化（commit 93a6224）**
   - 契機: 別セッション（at002/global）で codex 再レビューが 3回中2回失敗（9.2MBログ洪水 / 9分タイムアウト）→ ユーザー「sd003に反映しろ sd003のミスだ」
   - 根本原因（実機確認）: sd003 自体に `2>&1 | tee` バグは無かった（grep ヒットは `command -v codex >/dev/null 2>&1` の存在確認）。真の「sd003のミス」は**framework が正準 codex-dispatch レシピを所有していなかった**こと。誤レシピが user-global スキルに野放しで存在していた
   - 実装: `.claude/skills/codex-dispatch/{SKILL.md, codex-run.sh}` を新規作成
     - 正準レシピ: `2>&1|tee` 禁止 / `-o` で最終回答 / `2> progress.log` / `-c model_reasoning_effort=medium --ignore-user-config` / `--sandbox read-only` / `RUST_LOG=error`
     - codex-run.sh = 決定論入口ラッパー（OOM/単一インスタンス preflight・出力検証・盲目リトライ禁止）
   - 配備: deploy.ps1 Phase 4-4（skills ツリーコピー）で全PJに自動配備（deploy.ps1 改変不要）。sync で `.agents/skills/codex-dispatch/` 同期（agy frontmatter `disable-model-invocation:true` 付与確認）

3. **auto-memory 追記**: `project_sd_upgrade_keep_optout`, `reference_codex_dispatch_recipe`（MEMORY.md 索引更新）

### 進行中
なし

### 未解決
1. **at002 本体の復旧**: 別セッションで対応中（本セッションは触れていない）。復旧後 at002 に `.sd003-keep`（CLAUDE.md / .claude/rules/ / .claude/hooks/ / .claude/skills/registry.json / package.json）を置くと再発防止になる

### 作成・変更ファイル

**sd003 リポジトリ（commit済み）**:
- `.claude/skills/sd-deploy/{deploy.ps1, deploy.sh, SKILL.md}`（14230fd）
- `.claude/skills/sd-upgrade/{upgrade.ps1, upgrade.sh, SKILL.md}`（14230fd）
- `.agents/skills/{sd-deploy,sd-upgrade}/*`（14230fd・sync ミラー）
- `.claude/skills/codex-dispatch/{SKILL.md, codex-run.sh}`（93a6224・新規）
- `.agents/skills/codex-dispatch/{SKILL.md, codex-run.sh}`（93a6224・sync ミラー）

**auto-memory（リポジトリ外）**:
- `~/.claude/projects/D--claudecode-sd003/memory/project_sd_upgrade_keep_optout.md`（新規）
- `~/.claude/projects/D--claudecode-sd003/memory/reference_codex_dispatch_recipe.md`（新規）
- `MEMORY.md`（索引2行追加）

**未コミット（意図的に除外）**:
- `.claude/settings.local.json`（ローカル設定・M）
- `skills_out.txt`（ルート直下 stray・P2）

### 使用した外部ファイル
- `~/.claude/skills/codex-dispatch/SKILL.md`（user-global codex-dispatch・別セッションの修正版を参照元として確認）
- `~/.claude/skills/codex-dispatch/codex-run.sh`（同上・sd003版のベース）
- （ユーザーが貼り付けた別セッションの transcript 2件: at002 sd-upgrade RCA / codex-dispatch 改善設計。ファイルではなくチャット貼付）

### 次回タスク

#### P0（緊急）
なし

#### P1（重要）
1. **workflow-impl / agent-review.sh の codex 呼び出しを正準レシピに整合**: 現状 `codex exec ... 2>/dev/null`（progress 破棄・`-o` なし・effort 未制御）。バグではないが正準化と不整合。auto-chain に触れるため未着手
2. **gemini-dispatch の sd003 反映検討**: 並行 global スキル。codex-dispatch と同様に正準化して sd003 に取り込むか検討（未調査）

#### P2（通常）
1. `skills_out.txt`（ルート直下 stray）整理
2. `.codex-review-result.md`（ルート直下 stray・旧codex出力）整理
3. 古い worktree `.claude/worktrees/epic-sutherland-41d93f/` の整理（重複ファイル源）
4. 他PJ（oc001/at001等）への sd-upgrade dry-run sweep（固有化 divergence の有無確認・read-only）
5. セッションアーカイブ: at002 旧セッション1件（約15MB）が `/archive-sessions --execute` 対象

### 備考

- **2コミット**: 14230fd（sd-upgrade .sd003-keep）, 93a6224（codex-dispatch）。どちらも master 直コミット（ユーザー選択）。両 commit 後に post-commit hook が `.sd/` 消失を検知し HEAD から36ファイル自動復元（既知ランタイムバグ・hook が機能。本セッションの commit には .sd/ ファイルは含まれない）
- **at002 は別セッション管轄**: ユーザー明言「at002 は別で処理しているので大丈夫」。本セッションは sd003（司令塔・source of truth）側の恒久対策に専念
- **設計思想**: 両修正とも「guardrails over rules」（記憶頼みでなく物理化）。.sd003-keep = オプトアウト物理化、codex-run.sh = 決定論入口、dry-run 正直化 = 誤報の構造的根絶
- **検証規律**: 貼付された RCA/報告を鵜呑みにせず実機（コード/disk）で確認してから着手（root-cause-first / 「実機>報告」）。codex-dispatch では「sd003に 2>&1|tee バグがある」仮説が実機確認で否定され、真因（正準レシピ不在）に到達

### 学習ナッジ（修正が2回以上ある場合のみ記載）
- 修正2回検出
- 修正内容:
  1. 「事故を起こしている」: 前セッションの sd-upgrade が at002 を踏み潰した件。誇張した安全報告（UPGRADE OK/無傷）への指摘 → 報告を正直化する設計（divergence 可視化）で恒久対応した
  2. 「sd003に反映しろ sd003のミスだ」: codex-dispatch を global/別セッション管轄と捉えて確認過多になっていた所を、ユーザーが「sd003 の責務」と明確化 → sd003 を source of truth として即反映する方針に修正
- 永続化提案: 「別セッションの作業を sd003 に貼られたら、原則 sd003（司令塔）が吸収すべき責務として扱う／確認過多より実機確認→即反映」を feedback memory 化を検討（auto-memory）
