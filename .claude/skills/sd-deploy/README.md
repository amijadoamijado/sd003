# SD003フレームワーク展開

現在の手順は [SKILL.md](SKILL.md) を参照する。コピーと検証は `deploy.ps1` / `deploy.sh` が行う。

新規導入は `/sd-deploy`、既存環境の更新は `/sd-upgrade` を使う。dry-runで対象を確認し、固有設定は `.sd003-keep` で保護する。

旧版の手動コピー手順はSD003リポジトリの `docs/archive/prompt-audit-20260926/sd-deploy-README.md` に履歴として保存した。現在の実行手順には使用しない。
