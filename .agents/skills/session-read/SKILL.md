---
name: session-read
description: Legacy alias for `sessionread`. Use when the user invokes `/sessionread` or `session-read` in Codex.
---

# session-read

この skill は `sessionread` の互換エイリアスです。
`sessionread` と同じ手順で `/sessionread` を再現してください。
